import styled from "styled-components";

const InputCheck = styled.input`
	position: absolute;
	width: 100%;
	height: 100%;
	top: 0;
	left: 0;
	opacity: 0;
	cursor: pointer;
`;

export default InputCheck;
